image: busybox

pages:
  stage: deploy
  script:
    - apk add --no-cache sass
    - sass src/style.scss public/style.css
    - echo "Site will be deployed to $CI_PAGES_URL"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
